# アプリケーション更新・修正手順

公開後のアプリケーションを修正し、本番サーバーに反映させる手順（デプロイフロー）です。

## 1. PCでの作業（開発・修正）

1.  VSCodeでコードを修正します。
2.  ローカルで動作確認をします。
3.  **Gitにコミットしてプッシュ**します。
    （ターミナルで以下を実行、またはVSCodeのGit機能を使用）
    ```bash
    git add .
    git commit -m "修正内容のメモ"
    git push origin main
    ```

## 2. サーバーでの作業（反映）

1.  **ConoHaのコンソール**（黒い画面）を開き、ログインします。
2.  アプリのフォルダに移動します。
    ```bash
    cd /var/www/majong-app
    ```
3.  GitHubから最新のコードを取り込みます。
    ```bash
    git pull
    ```
4.  アプリをビルド（構築）し直します。
    ```bash
    npm run build
    ```
5.  アプリを再起動して反映させます。
    ```bash
    pm2 restart majong
    ```

## まとめ（コピペ用コマンド）
サーバーに接続後、以下をまとめて実行すれば一発で完了します。

```bash
cd /var/www/majong-app && git pull && npm run build && pm2 restart majong
```

## ⚠️ 重要なデータについて（データベース）
データベースファイルは、アプリ本体とは別の場所（`/var/www/majong-data/`）に保存されています。
そのため、**アプリを更新したり再起動したりしても、登録データ（ユーザー、成績など）が消えることはありません。**
安心して更新作業を行ってください。

